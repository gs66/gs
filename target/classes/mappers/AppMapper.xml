<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC  
    "-//mybatis.org//DTD Mapper 3.0//EN"  
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
   
<mapper namespace="com.fh.shop.api.app.mapper.IAppMapper">

<select id="findSecret" resultType="string" parameterType="string">
select appSecret from t_app where appKey=#{value}
</select>

</mapper>